---
"@audio-ui/react": patch
"@audio-ui/utils": patch
---

Improve knob interaction consistency in `@audio-ui/react` with refined drag mode behavior, wheel guard updates, and double-tap/double-click reset to `defaultValue`.

Add `BiProcedure<T, U>` to `@audio-ui/utils` for two-argument callback typing used by updated interaction hooks.
